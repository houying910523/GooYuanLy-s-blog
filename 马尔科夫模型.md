# 马尔可夫模型

1. 马尔科夫模型是一个用于预测的统计模型
2. 适用场景：处理序列分类问题
3. 应用：词类标注，语音识别，句子切分，信息抽取等

## 马尔可夫过程

1. 马尔科夫过程是一个离散随机过程，在这个过程中，t+1时刻的状态出现的概率只与t时刻的状态有关，与更早时刻的状态无关。

2. 时间和状态都是离散的马尔科夫过程，称为马尔科夫链

## 马尔可夫链

1. 基本概念
    在随机过程中，每个语言符号的出现概率不相互独立，每个随机试验的当前状态依赖于此前状态，这种链就是马尔可夫链。
2. 数学表示
    $P(X_{n+1} = x|X_{0},X_{1},X_{2}...X_{n}) = P(X_{n+1} = x|X_{n})$

    对于有N个状态的一阶马尔科夫模型，每个状态可以转移到另一个状态（包括自己），则共有N^2次状态转移，可以用状态转移矩阵表示。
    > $P_{ij} = P(X_{n + 1} = i | X_{n} = j)$，    其中Pij表示系统在时刻t处于状态j，在下一时刻t+1处于状态i的概率。

3. 马尔可夫模型是对有限状态机的扩充
4. 马尔可夫链在数学上描述了自然语言句子生成的过程，是一个早期的自然语言形式的模型。

# 隐含马尔可夫模型

在马尔可夫模型中，每一个状态都是可观察的序列，是状态关于时间的随机过程，也成为可视马尔可夫模型

当我们处理的事件不是直接从观察中得到的，而是隐含在一系列观察得到的事件背后，诸如词类标注，语音识别等。类似这样的事件，观察到的状态序列和隐藏的状态序列是概率相关的，于是我们可以将这种类似的过程建模为有一个隐含的马尔可夫模型和一个与这个隐含的马尔可夫过程概率相关的并且可以观察到的状态集合，这就是隐含马尔可夫模型。

在隐马模型中，观察值是关于状态的随机过程，而状态是关于时间的随机过程，因此隐马模型是一个双重随机过程。

## 模型要素描述

1. 状态集合 $Q=\{q_{1},q_{2},...\}（隐含状态）$
2. 观测集合 $V=\{v_{1},v_{2},...\}（观测状态）$
3. 状态转移概率矩阵 $A=\left[a_{ij}\right], a_{ij} 表示任意时刻t状态q_{i}下，下一时刻t+1状态为q_{j}的概率$
4. 观测概率 $B=\left[b_{ij}\right]即在某一时刻t的隐含状态q_{i}下，生成观测值v_{j}的概率$
5. 初始状态概率向量 $P=\{P_{1},P_{2},...\} Pi表示初始时刻t=1时，状态为q_{i}的概率$

一个隐含马尔可夫模型由以上五点要素来描述。

## HMM讨论的三个基本问题

1. $给定模型\lambda = (A, B, P)，计算其产生观测序列 O=(o_{1},o_{2},...,o_{t})的概率P(O|\lambda)。$
2. $给定模型\lambda = (A, B, P), 和观测序列O=(o_{1},o_{2},...,o_{t})，推断能产生此观测序列的最大似然状态序列。$
3. $给定观测序列O=(o1,o2,...,ot)，估计模型\lambda = (A, B, P)的参数，使P(O|\lambda)最大。$

## 三个问题的基本解法

### 问题一的解法：
#### 前向算法（t=1，一步一步向前计算）
+ 输入：隐含马尔可夫模型 $\lambda$，观测序列O;
+ 输出：观测序列概率 $P(O|\lambda)$
+ 过程：
    1. 初值：
    $$ \alpha_{1}(i)=P_{i}b_{i}(o_{1}), i=1,2,...,N $$
    2. 递推： 对t=1,2,...,T-1,
    $$ \alpha_{t+1}(i)= \left[ \sum \alpha_{t}(j) * a_{ji} \right] * b_{i}(o_{t+1}) $$
    3. 终止：
    $$ P(O|\lambda) = \sum \alpha_{r}(i) $$
+ 解释:
    1. 初始化向前概率，即初始时刻的状态 $q_{i}$ 和观测 $o_{1}$ 的联合概率，又因为到达初始时刻 $q_{i}$ 的概率是 $P_{i}$ ,根据初始状态 $q_{i}$ 产生的观测 $o_{1}$ 的概率是 $b_{i}(o_{1})$ ，于是有：
    $$ \alpha_{1}(i)=P_{i}b_{i}(o_{1}), i=1,2,...,N $$
    2. 向前概率的递推公式，计算到t+1时刻部分观测序列为$o_{1},o_{2},...,o_{t},o_{t+1}$ 且在t+1时刻处于状态 $q_{i}的向前概率$ ，其中：
    $\alpha_{t}(j)$ 表示到t时刻观测到 $o_{1},o_{2},...,o_{t}$ 并在时刻t处于状态 $q_{j}$ 的向前概率。
    乘积 $\alpha_{t}(j) * a_{ji}$ 表示t时刻观测到 $o_{1},o_{2},...,o_{t}$ 并在时刻t处于状态 $q_{j}$ 而时刻t+1到达状态 $q_{i}$ 的联合概率，对这个乘积在时刻t的所有可能的N的状态 $q_{j}$ 求和，其结果就是到时刻t观测为 $o_{1}, o_{2}, ..., o_{t}$ 并在时刻t+1处于状态 $q_{i}$ 的联合概率。
    3. $a_{t}(i)=P(o_{1},o_{2},...,o_{T},i_{T}=q_{i}|\lambda)$ 表示到达时刻T观测到$o_{1},o_{2},...,o_{T}$ 并在时刻T处于状态 $q_{i}$ 的向前概率，于是对所有的状态求和，就是观测序列概率P(O|λ)的计算公式了
+ 说明: 
    前向算法高效的关键是其局部计算前向概率，然后利用路径结构将前向概率“递推”到全局，得到P(O|λ)。
+ 矩阵表示：
    $$ \alpha_{1}=P*B(O_{1}) \\
    \alpha_{i}=\alpha_{i-1} * A * B(O_{i}) $$
+ 例子：
考虑盒子和球模型 $\lambda=(A, B, P)$，状态集合Q={1,2,3}，观测集合V={红，白}，
    $ A = \begin{bmatrix}
            0.5 & 0.2 & 0.3 \\
            0.3 & 0.5 & 0.2 \\
            0.2 & 0.3 & 0.5
        \end{bmatrix} $
    $ B = \begin{bmatrix}
            0.5 & 0.5 \\
            0.4 & 0.6 \\
            0.7 & 0.3
        \end{bmatrix} $
    $ P = (0.2, 0.4, 0.4)^{T} $
    设T=3, O=(红，白，红)，试用向前算法计算 $P=(O|\lambda)$
> 解：
1. 计算初值
    $ \alpha_{1}(1)=P_{1} * b_{1}(o_{1}) = 0.2 * 0.5 = 0.10 $ 初始状态=1，且生成红色的概率
    $ \alpha_{1}(2)=P_{2} * b_{2}(o_{1}) = 0.4 * 0.4 = 0.16 $ 初始状态=2，且生成红色的概率
    $ \alpha_{1}(3)=P_{3} * b_{3}(o_{1}) = 0.4 * 0.7 = 0.28 $ 初始状态=3，且生成红色的概率
2. 递推计算

    | 状态 | $\alpha_{2}(q)$ | $\alpha_{3}(q)$ |
    |:----|----|----|----|
    |1  | $(\alpha_{1}(1) * a_{11} + \alpha_{1}(2) * a_{21} + \alpha_{1}(3) * a_{31}) * b_{1}(o_{2})$| $(\alpha_{2}(1) * a_{11} + \alpha_{2}(2) * a_{21} + \alpha_{2}(3) * a_{31}) * b_{1}(o_{3})$|
    |2  | $(\alpha_{1}(1) * a_{12} + \alpha_{1}(2) * a_{22} + \alpha_{1}(3) * a_{32}) * b_{2}(o_{2})$| $(\alpha_{2}(1) * a_{12} + \alpha_{2}(2) * a_{22} + \alpha_{2}(3) * a_{32}) * b_{2}(o_{3})$|
    |3  | $(\alpha_{1}(1) * a_{13} + \alpha_{1}(2) * a_{23} + \alpha_{1}(3) * a_{33}) * b_{3}(o_{2})$| $(\alpha_{2}(1) * a_{13} + \alpha_{2}(2) * a_{23} + \alpha_{2}(3) * a_{33}) * b_{3}(o_{3})$|

3. 终止
    $ P(O|\lambda) = \sum \alpha_{3}(i) = 0.13022 $

#### 后向算法（t=T, 一步一步向后计算）
+ 输入：隐含马尔可夫模型 $\lambda$，观测序列O
+ 输出：观测序列概率 $P(O|\lambda)$
+ 解释：
    定义$\beta_{t}(i)$为在时刻 t 处在状态 i 的条件下，后续观察到ot+1,...,oT的概率，即
    $$\beta_{t}(i)=P(o_{t+1},...,o_{T}|q_{t}=i,\lambda)$$
    则
    $$\beta_{t+1}(j)=P(o_{t+2},...,o_{T}|q_{t+1}=j,\lambda)$$
    则
    $$ \alpha_{ij} * b_{i}(o_{t+1})*P(o_{t+2},...,o_{T}|q_{t+1}=j,\lambda)= P(o_{t+1},...,o_{T}|q_{t}=i,q_{t+1}=j|\lambda) \\
     \sum_{j=1..N} \alpha_{ij} * b_{i}(o_{t+1}) * P(o_{t+2},...,o_{T}|q_{t+1}=j|\lambda) = P(o_{t+1},...,o_{T}|q_{t}=i,\lambda) $$
    即 
    $$ \beta_{t}(i) = \sum_{j=1..N} a_{ij} *  b_{j}(o_{t+1}) * \beta_{t+1}(j) $$
    
+ 步骤：
    1. 初始化后向概率，对最终时刻的所有状态$q_{i}$规定$\beta_{t}(i)=1$。
    2. 后向概率递推公式
    为了计算在t时刻状态为$q_{i}$条件下时刻t+1之后的观测序列为$o_{t+1},o_{t+1},...,o_{T}$的后向概率$\beta_{t}(i)$，只需考虑在时刻t+1所有可能的N个状态$q_{j}$的转移概率（即$a_{ij}$项），以及在此状态下的观测概率（即$b_{j}(o_{t+1})$项），和概率状态$q_{j}$之后的观测序列的后向概率（即$\beta_{t+1}(i)$项）。
    3. 求P(O|λ)的思路与步骤2一直，只是用初始概率$p_{i}$代替转移概率。

+ 过程：
    1. 初始化后向概率，对最终时刻的所有状态$q_{i}$规定$\beta_{T}(i)=1$
    2. 递推t=T-1,T-2,...,1
        $\beta_{t}(i) = \sum_{j=1..N} a_{ij} *  b_{j}(o_{t+1}) * \beta_{t+1}(j) $
    3. 终止
        $p(O|\lambda) = \sum_{i=1..N} \beta_{1}(i) * p_{i} * b_{i1}$
即 $\beta_{t}=B * A^{T} * \beta_{t+1}$

### 问题二的解法（维特比算法）：
